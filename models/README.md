# models

Generated model artifacts are scoped by module.

- `lol/`: League of Legends trained models, feature pipelines, rating tables, and metadata.

The files in this tree are generated and ignored by Git, except README files.
