# data/lol

Generated League of Legends datasets for `lol-bets`.

- `raw/`: downloaded Oracle's Elixir data.
- `interim/`: cleaned team and player tables.
- `processed/`: enriched training and inference artifacts.

Example:

```bash
uv run oracle-bets lol ingest
```
