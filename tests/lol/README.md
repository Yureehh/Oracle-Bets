# tests/lol

Unit and smoke tests for the current League of Legends suite.

Coverage focuses on package wiring, artifact health, betting math, market
normalization, ingestion pairing, and leak-free feature generation.

Example:

```bash
uv run --extra dev pytest -q
```
