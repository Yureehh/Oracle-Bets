# tests

Tests are scoped by module.

- `lol/`: current unit and smoke tests for Oracle Bets core wiring, Discord registry, markets, and `lol-bets`.

Example:

```bash
uv run --extra dev pytest -q
```
