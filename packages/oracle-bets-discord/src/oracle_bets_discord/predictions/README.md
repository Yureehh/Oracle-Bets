# predictions

Discord presentation helpers for LoL prediction commands.

This folder formats schedules, profiles, best-of series probabilities, and
prediction responses. It should not own model logic.

Example:

```python
from oracle_bets_discord.predictions.discord_utils import validate_and_predict
```
