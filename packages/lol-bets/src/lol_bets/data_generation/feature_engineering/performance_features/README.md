# performance_features

Historical performance features for teams and players.

These modules compute leak-free `*_before` and current-state `*_after` EMA
features for entity stats, side win rate, patch win rate, and season win rate.

Example:

```python
from lol_bets.data_generation.feature_engineering.performance_features.performance_metrics import PerformanceMetrics
```
